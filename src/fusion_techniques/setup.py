from setuptools import setup, find_packages

setup(
    name="fusion_techniques",
    version="0.1",
    description="Fusion techniques for multimodal TERS",
    author="Your Name",
    packages=find_packages(),
    install_requires=["torch"],
)
